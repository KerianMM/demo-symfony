{% extends 'base.html.twig' %}

{% block title %}Edit Style{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('style_edit') }}
{% endblock %}

{% block body %}
    <h1>Edit Style</h1>

    {{ include('style/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('style_index') }}" class="btn btn-success">back to list</a>

    {{ include('style/_delete_form.html.twig') }}
{% endblock %}
